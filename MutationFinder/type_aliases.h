#ifndef TYPE_ALIASES
#define TYPE_ALIASES

//Aliases
#include <string>
using String = std::string;

#include <fstream>
using InputFileStream = std::ifstream;

#include <vector>
using CharVector = std::vector<char>;

#endif // TYPE_ALIASES
